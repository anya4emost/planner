import React, { ReactNode } from 'react'

const SplitterWrapper = ({ children }: { children: ReactNode }) => {
  return (
    <div>
          {children}
    </div>
  )
}

export default SplitterWrapper
